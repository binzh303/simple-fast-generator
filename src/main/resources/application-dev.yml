spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/simple_fast?serverTimezone=CTT&useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: zby303
mybatis:
  mapper-locations: classpath:mapper/*.xml